version: "3.9"

services:
  web:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - ./data:/data
    environment:
      DATABASE_URL: sqlite:////data/budget.db
    restart: unless-stopped
