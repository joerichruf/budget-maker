services:
  web:
    image: ${DOCKER_IMAGE:-budget-maker}
    build: .
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ${DATA_DIR:-./data}:/data
    env_file:
      - .env
    restart: unless-stopped
